#!/bin/bash

user=root

user_count=$(sudo -u $user prlctl list -a | grep -oE '\{[a-f0-9\-]+\}' | wc -l | awk '{$1=$1};1')

if [[ $user_count ]]; then
echo "
<result>$user_count</result>"
else
echo "
<result>No VMs found</result>"
fi

exit 0